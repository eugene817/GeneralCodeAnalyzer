import { BaseToken } from "../../baseToken.js";
export declare class Line extends BaseToken {
    readonly text: string;
    constructor(lineNumber: number, text: string);
    equals<T extends BaseToken>(other: T): boolean;
    toString(): string;
}
