import { BaseToken } from "../../baseToken.js";
import { Line } from "../../linesCodec/tokens/line.js";
export declare class Hash extends BaseToken {
    static readonly symbol: string;
    get text(): string;
    static newOnLine(line: Line, atColumnNumber: number): Hash;
    toString(): string;
}
