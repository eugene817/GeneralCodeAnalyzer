import { BaseToken } from "../../baseToken.js";
import { Line } from "../../linesCodec/tokens/line.js";
export declare class LeftParenthesis extends BaseToken {
    static readonly symbol: string;
    get text(): string;
    static newOnLine(line: Line, atColumnNumber: number): LeftParenthesis;
    toString(): string;
}
export declare class RightParenthesis extends BaseToken {
    static readonly symbol: string;
    get text(): string;
    static newOnLine(line: Line, atColumnNumber: number): RightParenthesis;
    toString(): string;
}
