import { IRange, Range } from "@codingame/monaco-vscode-api/vscode/vs/editor/common/core/range";
import { BaseToken } from "../../../../../../../editor/common/codecs/baseToken.js";
import { Word } from "../../../../../../../editor/common/codecs/simpleCodec/tokens/word.js";
export declare class FileReference extends BaseToken {
    readonly path: string;
    static readonly TOKEN_START: string;
    constructor(range: Range, path: string);
    get text(): string;
    static fromWord(word: Word): FileReference;
    equals<T extends BaseToken>(other: T): boolean;
    get linkRange(): IRange | undefined;
    toString(): string;
}
