
import { ChatPromptDecoder } from './chatPromptDecoder.js';

const ChatPromptCodec = ( Object.freeze({
    encode: (_stream) => {
        throw ( new Error('The `encode` method is not implemented.'));
    },
    decode: (stream) => {
        return ( new ChatPromptDecoder(stream));
    },
}));

export { ChatPromptCodec };
