
import { __decorate, __param } from '@codingame/monaco-vscode-api/external/tslib/tslib.es6';
import { localize } from '@codingame/monaco-vscode-api/vscode/vs/nls';
import { parse, getNodeType } from '@codingame/monaco-vscode-api/vscode/vs/base/common/json';
import { isObject } from '@codingame/monaco-vscode-api/vscode/vs/base/common/types';
import { IndentAction } from '@codingame/monaco-vscode-api/vscode/vs/editor/common/languages/languageConfiguration';
import { ILanguageConfigurationService } from '@codingame/monaco-vscode-api/vscode/vs/editor/common/languages/languageConfigurationRegistry.service';
import { ILanguageService } from '@codingame/monaco-vscode-api/vscode/vs/editor/common/languages/language.service';
import { Extensions } from '@codingame/monaco-vscode-api/vscode/vs/platform/jsonschemas/common/jsonContributionRegistry';
import { Registry } from '@codingame/monaco-vscode-api/vscode/vs/platform/registry/common/platform';
import { IExtensionService } from '@codingame/monaco-vscode-api/vscode/vs/workbench/services/extensions/common/extensions.service';
import { getParseErrorMessage } from '@codingame/monaco-vscode-api/vscode/vs/base/common/jsonErrorMessages';
import { IExtensionResourceLoaderService } from '@codingame/monaco-vscode-api/vscode/vs/platform/extensionResourceLoader/common/extensionResourceLoader.service';
import { hash } from '@codingame/monaco-vscode-api/vscode/vs/base/common/hash';
import { Disposable } from '@codingame/monaco-vscode-api/vscode/vs/base/common/lifecycle';
import { registerWorkbenchContribution2, WorkbenchPhase } from '@codingame/monaco-vscode-api/vscode/vs/workbench/common/contributions';
import { IInstantiationService } from '@codingame/monaco-vscode-api/vscode/vs/platform/instantiation/common/instantiation';

var LanguageConfigurationFileHandler_1;
function isStringArr(something) {
    if (!Array.isArray(something)) {
        return false;
    }
    for (let i = 0, len = something.length; i < len; i++) {
        if (typeof something[i] !== 'string') {
            return false;
        }
    }
    return true;
}
function isCharacterPair(something) {
    return (isStringArr(something)
        && something.length === 2);
}
let LanguageConfigurationFileHandler = LanguageConfigurationFileHandler_1 = class LanguageConfigurationFileHandler extends Disposable {
    constructor(_languageService, _extensionResourceLoaderService, _extensionService, _languageConfigurationService) {
        super();
        this._languageService = _languageService;
        this._extensionResourceLoaderService = _extensionResourceLoaderService;
        this._extensionService = _extensionService;
        this._languageConfigurationService = _languageConfigurationService;
        this._done = ( new Map());
        this._register(this._languageService.onDidRequestBasicLanguageFeatures(async (languageIdentifier) => {
            this._extensionService.whenInstalledExtensionsRegistered().then(() => {
                this._loadConfigurationsForMode(languageIdentifier);
            });
        }));
        this._register(this._languageService.onDidChange(() => {
            for (const [languageId] of this._done) {
                this._loadConfigurationsForMode(languageId);
            }
        }));
    }
    async _loadConfigurationsForMode(languageId) {
        const configurationFiles = this._languageService.getConfigurationFiles(languageId);
        const configurationHash = hash(( configurationFiles.map(uri => ( uri.toString()))));
        if (this._done.get(languageId) === configurationHash) {
            return;
        }
        this._done.set(languageId, configurationHash);
        const configs = await Promise.all(( configurationFiles.map(configFile => this._readConfigFile(configFile))));
        for (const config of configs) {
            this._handleConfig(languageId, config);
        }
    }
    async _readConfigFile(configFileLocation) {
        try {
            const contents = await this._extensionResourceLoaderService.readExtensionResource(configFileLocation);
            const errors = [];
            let configuration = parse(contents, errors);
            if (errors.length) {
                console.error(( localize(
                    4973,
                    "Errors parsing {0}: {1}",
                    (configFileLocation.toString()),
                    ( errors.map(e => (`[${e.offset}, ${e.length}] ${getParseErrorMessage(e.error)}`))).join('\n')
                )));
            }
            if (getNodeType(configuration) !== 'object') {
                console.error(( localize(
                    4974,
                    "{0}: Invalid format, JSON object expected.",
                    (configFileLocation.toString())
                )));
                configuration = {};
            }
            return configuration;
        }
        catch (err) {
            console.error(err);
            return {};
        }
    }
    static _extractValidCommentRule(languageId, configuration) {
        const source = configuration.comments;
        if (typeof source === 'undefined') {
            return undefined;
        }
        if (!isObject(source)) {
            console.warn(`[${languageId}]: language configuration: expected \`comments\` to be an object.`);
            return undefined;
        }
        let result = undefined;
        if (typeof source.lineComment !== 'undefined') {
            if (typeof source.lineComment !== 'string') {
                console.warn(`[${languageId}]: language configuration: expected \`comments.lineComment\` to be a string.`);
            }
            else {
                result = result || {};
                result.lineComment = source.lineComment;
            }
        }
        if (typeof source.blockComment !== 'undefined') {
            if (!isCharacterPair(source.blockComment)) {
                console.warn(`[${languageId}]: language configuration: expected \`comments.blockComment\` to be an array of two strings.`);
            }
            else {
                result = result || {};
                result.blockComment = source.blockComment;
            }
        }
        return result;
    }
    static _extractValidBrackets(languageId, configuration) {
        const source = configuration.brackets;
        if (typeof source === 'undefined') {
            return undefined;
        }
        if (!Array.isArray(source)) {
            console.warn(`[${languageId}]: language configuration: expected \`brackets\` to be an array.`);
            return undefined;
        }
        let result = undefined;
        for (let i = 0, len = source.length; i < len; i++) {
            const pair = source[i];
            if (!isCharacterPair(pair)) {
                console.warn(`[${languageId}]: language configuration: expected \`brackets[${i}]\` to be an array of two strings.`);
                continue;
            }
            result = result || [];
            result.push(pair);
        }
        return result;
    }
    static _extractValidAutoClosingPairs(languageId, configuration) {
        const source = configuration.autoClosingPairs;
        if (typeof source === 'undefined') {
            return undefined;
        }
        if (!Array.isArray(source)) {
            console.warn(`[${languageId}]: language configuration: expected \`autoClosingPairs\` to be an array.`);
            return undefined;
        }
        let result = undefined;
        for (let i = 0, len = source.length; i < len; i++) {
            const pair = source[i];
            if (Array.isArray(pair)) {
                if (!isCharacterPair(pair)) {
                    console.warn(`[${languageId}]: language configuration: expected \`autoClosingPairs[${i}]\` to be an array of two strings or an object.`);
                    continue;
                }
                result = result || [];
                result.push({ open: pair[0], close: pair[1] });
            }
            else {
                if (!isObject(pair)) {
                    console.warn(`[${languageId}]: language configuration: expected \`autoClosingPairs[${i}]\` to be an array of two strings or an object.`);
                    continue;
                }
                if (typeof pair.open !== 'string') {
                    console.warn(`[${languageId}]: language configuration: expected \`autoClosingPairs[${i}].open\` to be a string.`);
                    continue;
                }
                if (typeof pair.close !== 'string') {
                    console.warn(`[${languageId}]: language configuration: expected \`autoClosingPairs[${i}].close\` to be a string.`);
                    continue;
                }
                if (typeof pair.notIn !== 'undefined') {
                    if (!isStringArr(pair.notIn)) {
                        console.warn(`[${languageId}]: language configuration: expected \`autoClosingPairs[${i}].notIn\` to be a string array.`);
                        continue;
                    }
                }
                result = result || [];
                result.push({ open: pair.open, close: pair.close, notIn: pair.notIn });
            }
        }
        return result;
    }
    static _extractValidSurroundingPairs(languageId, configuration) {
        const source = configuration.surroundingPairs;
        if (typeof source === 'undefined') {
            return undefined;
        }
        if (!Array.isArray(source)) {
            console.warn(`[${languageId}]: language configuration: expected \`surroundingPairs\` to be an array.`);
            return undefined;
        }
        let result = undefined;
        for (let i = 0, len = source.length; i < len; i++) {
            const pair = source[i];
            if (Array.isArray(pair)) {
                if (!isCharacterPair(pair)) {
                    console.warn(`[${languageId}]: language configuration: expected \`surroundingPairs[${i}]\` to be an array of two strings or an object.`);
                    continue;
                }
                result = result || [];
                result.push({ open: pair[0], close: pair[1] });
            }
            else {
                if (!isObject(pair)) {
                    console.warn(`[${languageId}]: language configuration: expected \`surroundingPairs[${i}]\` to be an array of two strings or an object.`);
                    continue;
                }
                if (typeof pair.open !== 'string') {
                    console.warn(`[${languageId}]: language configuration: expected \`surroundingPairs[${i}].open\` to be a string.`);
                    continue;
                }
                if (typeof pair.close !== 'string') {
                    console.warn(`[${languageId}]: language configuration: expected \`surroundingPairs[${i}].close\` to be a string.`);
                    continue;
                }
                result = result || [];
                result.push({ open: pair.open, close: pair.close });
            }
        }
        return result;
    }
    static _extractValidColorizedBracketPairs(languageId, configuration) {
        const source = configuration.colorizedBracketPairs;
        if (typeof source === 'undefined') {
            return undefined;
        }
        if (!Array.isArray(source)) {
            console.warn(`[${languageId}]: language configuration: expected \`colorizedBracketPairs\` to be an array.`);
            return undefined;
        }
        const result = [];
        for (let i = 0, len = source.length; i < len; i++) {
            const pair = source[i];
            if (!isCharacterPair(pair)) {
                console.warn(`[${languageId}]: language configuration: expected \`colorizedBracketPairs[${i}]\` to be an array of two strings.`);
                continue;
            }
            result.push([pair[0], pair[1]]);
        }
        return result;
    }
    static _extractValidOnEnterRules(languageId, configuration) {
        const source = configuration.onEnterRules;
        if (typeof source === 'undefined') {
            return undefined;
        }
        if (!Array.isArray(source)) {
            console.warn(`[${languageId}]: language configuration: expected \`onEnterRules\` to be an array.`);
            return undefined;
        }
        let result = undefined;
        for (let i = 0, len = source.length; i < len; i++) {
            const onEnterRule = source[i];
            if (!isObject(onEnterRule)) {
                console.warn(`[${languageId}]: language configuration: expected \`onEnterRules[${i}]\` to be an object.`);
                continue;
            }
            if (!isObject(onEnterRule.action)) {
                console.warn(`[${languageId}]: language configuration: expected \`onEnterRules[${i}].action\` to be an object.`);
                continue;
            }
            let indentAction;
            if (onEnterRule.action.indent === 'none') {
                indentAction = IndentAction.None;
            }
            else if (onEnterRule.action.indent === 'indent') {
                indentAction = IndentAction.Indent;
            }
            else if (onEnterRule.action.indent === 'indentOutdent') {
                indentAction = IndentAction.IndentOutdent;
            }
            else if (onEnterRule.action.indent === 'outdent') {
                indentAction = IndentAction.Outdent;
            }
            else {
                console.warn(`[${languageId}]: language configuration: expected \`onEnterRules[${i}].action.indent\` to be 'none', 'indent', 'indentOutdent' or 'outdent'.`);
                continue;
            }
            const action = { indentAction };
            if (onEnterRule.action.appendText) {
                if (typeof onEnterRule.action.appendText === 'string') {
                    action.appendText = onEnterRule.action.appendText;
                }
                else {
                    console.warn(`[${languageId}]: language configuration: expected \`onEnterRules[${i}].action.appendText\` to be undefined or a string.`);
                }
            }
            if (onEnterRule.action.removeText) {
                if (typeof onEnterRule.action.removeText === 'number') {
                    action.removeText = onEnterRule.action.removeText;
                }
                else {
                    console.warn(`[${languageId}]: language configuration: expected \`onEnterRules[${i}].action.removeText\` to be undefined or a number.`);
                }
            }
            const beforeText = this._parseRegex(languageId, `onEnterRules[${i}].beforeText`, onEnterRule.beforeText);
            if (!beforeText) {
                continue;
            }
            const resultingOnEnterRule = { beforeText, action };
            if (onEnterRule.afterText) {
                const afterText = this._parseRegex(languageId, `onEnterRules[${i}].afterText`, onEnterRule.afterText);
                if (afterText) {
                    resultingOnEnterRule.afterText = afterText;
                }
            }
            if (onEnterRule.previousLineText) {
                const previousLineText = this._parseRegex(languageId, `onEnterRules[${i}].previousLineText`, onEnterRule.previousLineText);
                if (previousLineText) {
                    resultingOnEnterRule.previousLineText = previousLineText;
                }
            }
            result = result || [];
            result.push(resultingOnEnterRule);
        }
        return result;
    }
    static extractValidConfig(languageId, configuration) {
        const comments = this._extractValidCommentRule(languageId, configuration);
        const brackets = this._extractValidBrackets(languageId, configuration);
        const autoClosingPairs = this._extractValidAutoClosingPairs(languageId, configuration);
        const surroundingPairs = this._extractValidSurroundingPairs(languageId, configuration);
        const colorizedBracketPairs = this._extractValidColorizedBracketPairs(languageId, configuration);
        const autoCloseBefore = (typeof configuration.autoCloseBefore === 'string' ? configuration.autoCloseBefore : undefined);
        const wordPattern = (configuration.wordPattern ? this._parseRegex(languageId, `wordPattern`, configuration.wordPattern) : undefined);
        const indentationRules = (configuration.indentationRules ? this._mapIndentationRules(languageId, configuration.indentationRules) : undefined);
        let folding = undefined;
        if (configuration.folding) {
            const rawMarkers = configuration.folding.markers;
            const startMarker = (rawMarkers && rawMarkers.start ? this._parseRegex(languageId, `folding.markers.start`, rawMarkers.start) : undefined);
            const endMarker = (rawMarkers && rawMarkers.end ? this._parseRegex(languageId, `folding.markers.end`, rawMarkers.end) : undefined);
            const markers = (startMarker && endMarker ? { start: startMarker, end: endMarker } : undefined);
            folding = {
                offSide: configuration.folding.offSide,
                markers
            };
        }
        const onEnterRules = this._extractValidOnEnterRules(languageId, configuration);
        const richEditConfig = {
            comments,
            brackets,
            wordPattern,
            indentationRules,
            onEnterRules,
            autoClosingPairs,
            surroundingPairs,
            colorizedBracketPairs,
            autoCloseBefore,
            folding,
            __electricCharacterSupport: undefined,
        };
        return richEditConfig;
    }
    _handleConfig(languageId, configuration) {
        const richEditConfig = LanguageConfigurationFileHandler_1.extractValidConfig(languageId, configuration);
        this._languageConfigurationService.register(languageId, richEditConfig, 50);
    }
    static _parseRegex(languageId, confPath, value) {
        if (typeof value === 'string') {
            try {
                return ( new RegExp(value, ''));
            }
            catch (err) {
                console.warn(`[${languageId}]: Invalid regular expression in \`${confPath}\`: `, err);
                return undefined;
            }
        }
        if (isObject(value)) {
            if (typeof value.pattern !== 'string') {
                console.warn(`[${languageId}]: language configuration: expected \`${confPath}.pattern\` to be a string.`);
                return undefined;
            }
            if (typeof value.flags !== 'undefined' && typeof value.flags !== 'string') {
                console.warn(`[${languageId}]: language configuration: expected \`${confPath}.flags\` to be a string.`);
                return undefined;
            }
            try {
                return ( new RegExp(value.pattern, value.flags));
            }
            catch (err) {
                console.warn(`[${languageId}]: Invalid regular expression in \`${confPath}\`: `, err);
                return undefined;
            }
        }
        console.warn(`[${languageId}]: language configuration: expected \`${confPath}\` to be a string or an object.`);
        return undefined;
    }
    static _mapIndentationRules(languageId, indentationRules) {
        const increaseIndentPattern = this._parseRegex(languageId, `indentationRules.increaseIndentPattern`, indentationRules.increaseIndentPattern);
        if (!increaseIndentPattern) {
            return undefined;
        }
        const decreaseIndentPattern = this._parseRegex(languageId, `indentationRules.decreaseIndentPattern`, indentationRules.decreaseIndentPattern);
        if (!decreaseIndentPattern) {
            return undefined;
        }
        const result = {
            increaseIndentPattern: increaseIndentPattern,
            decreaseIndentPattern: decreaseIndentPattern
        };
        if (indentationRules.indentNextLinePattern) {
            result.indentNextLinePattern = this._parseRegex(languageId, `indentationRules.indentNextLinePattern`, indentationRules.indentNextLinePattern);
        }
        if (indentationRules.unIndentedLinePattern) {
            result.unIndentedLinePattern = this._parseRegex(languageId, `indentationRules.unIndentedLinePattern`, indentationRules.unIndentedLinePattern);
        }
        return result;
    }
};
LanguageConfigurationFileHandler = LanguageConfigurationFileHandler_1 = ( __decorate([
    ( __param(0, ILanguageService)),
    ( __param(1, IExtensionResourceLoaderService)),
    ( __param(2, IExtensionService)),
    ( __param(3, ILanguageConfigurationService))
], LanguageConfigurationFileHandler));
const schemaId = 'vscode://schemas/language-configuration';
const schema = {
    allowComments: true,
    allowTrailingCommas: true,
    default: {
        comments: {
            blockComment: ['/*', '*/'],
            lineComment: '//'
        },
        brackets: [['(', ')'], ['[', ']'], ['{', '}']],
        autoClosingPairs: [['(', ')'], ['[', ']'], ['{', '}']],
        surroundingPairs: [['(', ')'], ['[', ']'], ['{', '}']]
    },
    definitions: {
        openBracket: {
            type: 'string',
            description: ( localize(4975, 'The opening bracket character or string sequence.'))
        },
        closeBracket: {
            type: 'string',
            description: ( localize(4976, 'The closing bracket character or string sequence.'))
        },
        bracketPair: {
            type: 'array',
            items: [{
                    $ref: '#/definitions/openBracket'
                }, {
                    $ref: '#/definitions/closeBracket'
                }]
        }
    },
    properties: {
        comments: {
            default: {
                blockComment: ['/*', '*/'],
                lineComment: '//'
            },
            description: ( localize(4977, 'Defines the comment symbols')),
            type: 'object',
            properties: {
                blockComment: {
                    type: 'array',
                    description: ( localize(4978, 'Defines how block comments are marked.')),
                    items: [{
                            type: 'string',
                            description: ( localize(4979, 'The character sequence that starts a block comment.'))
                        }, {
                            type: 'string',
                            description: ( localize(4980, 'The character sequence that ends a block comment.'))
                        }]
                },
                lineComment: {
                    type: 'string',
                    description: ( localize(4981, 'The character sequence that starts a line comment.'))
                }
            }
        },
        brackets: {
            default: [['(', ')'], ['[', ']'], ['{', '}']],
            markdownDescription: ( localize(
                4982,
                'Defines the bracket symbols that increase or decrease the indentation. When bracket pair colorization is enabled and {0} is not defined, this also defines the bracket pairs that are colorized by their nesting level.',
                '\`colorizedBracketPairs\`'
            )),
            type: 'array',
            items: {
                $ref: '#/definitions/bracketPair'
            }
        },
        colorizedBracketPairs: {
            default: [['(', ')'], ['[', ']'], ['{', '}']],
            markdownDescription: ( localize(
                4983,
                'Defines the bracket pairs that are colorized by their nesting level if bracket pair colorization is enabled. Any brackets included here that are not included in {0} will be automatically included in {0}.',
                '\`brackets\`'
            )),
            type: 'array',
            items: {
                $ref: '#/definitions/bracketPair'
            }
        },
        autoClosingPairs: {
            default: [['(', ')'], ['[', ']'], ['{', '}']],
            description: ( localize(
                4984,
                'Defines the bracket pairs. When a opening bracket is entered, the closing bracket is inserted automatically.'
            )),
            type: 'array',
            items: {
                oneOf: [{
                        $ref: '#/definitions/bracketPair'
                    }, {
                        type: 'object',
                        properties: {
                            open: {
                                $ref: '#/definitions/openBracket'
                            },
                            close: {
                                $ref: '#/definitions/closeBracket'
                            },
                            notIn: {
                                type: 'array',
                                description: ( localize(4985, 'Defines a list of scopes where the auto pairs are disabled.')),
                                items: {
                                    enum: ['string', 'comment']
                                }
                            }
                        }
                    }]
            }
        },
        autoCloseBefore: {
            default: ';:.,=}])> \n\t',
            description: ( localize(
                4986,
                'Defines what characters must be after the cursor in order for bracket or quote autoclosing to occur when using the \'languageDefined\' autoclosing setting. This is typically the set of characters which can not start an expression.'
            )),
            type: 'string',
        },
        surroundingPairs: {
            default: [['(', ')'], ['[', ']'], ['{', '}']],
            description: ( localize(
                4987,
                'Defines the bracket pairs that can be used to surround a selected string.'
            )),
            type: 'array',
            items: {
                oneOf: [{
                        $ref: '#/definitions/bracketPair'
                    }, {
                        type: 'object',
                        properties: {
                            open: {
                                $ref: '#/definitions/openBracket'
                            },
                            close: {
                                $ref: '#/definitions/closeBracket'
                            }
                        }
                    }]
            }
        },
        wordPattern: {
            default: '',
            description: ( localize(
                4988,
                'Defines what is considered to be a word in the programming language.'
            )),
            type: ['string', 'object'],
            properties: {
                pattern: {
                    type: 'string',
                    description: ( localize(4989, 'The RegExp pattern used to match words.')),
                    default: '',
                },
                flags: {
                    type: 'string',
                    description: ( localize(4990, 'The RegExp flags used to match words.')),
                    default: 'g',
                    pattern: '^([gimuy]+)$',
                    patternErrorMessage: ( localize(4991, 'Must match the pattern `/^([gimuy]+)$/`.'))
                }
            }
        },
        indentationRules: {
            default: {
                increaseIndentPattern: '',
                decreaseIndentPattern: ''
            },
            description: ( localize(4992, 'The language\'s indentation settings.')),
            type: 'object',
            properties: {
                increaseIndentPattern: {
                    type: ['string', 'object'],
                    description: ( localize(
                        4993,
                        'If a line matches this pattern, then all the lines after it should be indented once (until another rule matches).'
                    )),
                    properties: {
                        pattern: {
                            type: 'string',
                            description: ( localize(4994, 'The RegExp pattern for increaseIndentPattern.')),
                            default: '',
                        },
                        flags: {
                            type: 'string',
                            description: ( localize(4995, 'The RegExp flags for increaseIndentPattern.')),
                            default: '',
                            pattern: '^([gimuy]+)$',
                            patternErrorMessage: ( localize(4996, 'Must match the pattern `/^([gimuy]+)$/`.'))
                        }
                    }
                },
                decreaseIndentPattern: {
                    type: ['string', 'object'],
                    description: ( localize(
                        4997,
                        'If a line matches this pattern, then all the lines after it should be unindented once (until another rule matches).'
                    )),
                    properties: {
                        pattern: {
                            type: 'string',
                            description: ( localize(4998, 'The RegExp pattern for decreaseIndentPattern.')),
                            default: '',
                        },
                        flags: {
                            type: 'string',
                            description: ( localize(4999, 'The RegExp flags for decreaseIndentPattern.')),
                            default: '',
                            pattern: '^([gimuy]+)$',
                            patternErrorMessage: ( localize(5000, 'Must match the pattern `/^([gimuy]+)$/`.'))
                        }
                    }
                },
                indentNextLinePattern: {
                    type: ['string', 'object'],
                    description: ( localize(
                        5001,
                        'If a line matches this pattern, then **only the next line** after it should be indented once.'
                    )),
                    properties: {
                        pattern: {
                            type: 'string',
                            description: ( localize(5002, 'The RegExp pattern for indentNextLinePattern.')),
                            default: '',
                        },
                        flags: {
                            type: 'string',
                            description: ( localize(5003, 'The RegExp flags for indentNextLinePattern.')),
                            default: '',
                            pattern: '^([gimuy]+)$',
                            patternErrorMessage: ( localize(5004, 'Must match the pattern `/^([gimuy]+)$/`.'))
                        }
                    }
                },
                unIndentedLinePattern: {
                    type: ['string', 'object'],
                    description: ( localize(
                        5005,
                        'If a line matches this pattern, then its indentation should not be changed and it should not be evaluated against the other rules.'
                    )),
                    properties: {
                        pattern: {
                            type: 'string',
                            description: ( localize(5006, 'The RegExp pattern for unIndentedLinePattern.')),
                            default: '',
                        },
                        flags: {
                            type: 'string',
                            description: ( localize(5007, 'The RegExp flags for unIndentedLinePattern.')),
                            default: '',
                            pattern: '^([gimuy]+)$',
                            patternErrorMessage: ( localize(5008, 'Must match the pattern `/^([gimuy]+)$/`.'))
                        }
                    }
                }
            }
        },
        folding: {
            type: 'object',
            description: ( localize(5009, 'The language\'s folding settings.')),
            properties: {
                offSide: {
                    type: 'boolean',
                    description: ( localize(
                        5010,
                        'A language adheres to the off-side rule if blocks in that language are expressed by their indentation. If set, empty lines belong to the subsequent block.'
                    )),
                },
                markers: {
                    type: 'object',
                    description: ( localize(
                        5011,
                        'Language specific folding markers such as \'#region\' and \'#endregion\'. The start and end regexes will be tested against the contents of all lines and must be designed efficiently'
                    )),
                    properties: {
                        start: {
                            type: 'string',
                            description: ( localize(
                                5012,
                                'The RegExp pattern for the start marker. The regexp must start with \'^\'.'
                            ))
                        },
                        end: {
                            type: 'string',
                            description: ( localize(
                                5013,
                                'The RegExp pattern for the end marker. The regexp must start with \'^\'.'
                            ))
                        },
                    }
                }
            }
        },
        onEnterRules: {
            type: 'array',
            description: ( localize(5014, 'The language\'s rules to be evaluated when pressing Enter.')),
            items: {
                type: 'object',
                description: ( localize(5014, 'The language\'s rules to be evaluated when pressing Enter.')),
                required: ['beforeText', 'action'],
                properties: {
                    beforeText: {
                        type: ['string', 'object'],
                        description: ( localize(
                            5015,
                            'This rule will only execute if the text before the cursor matches this regular expression.'
                        )),
                        properties: {
                            pattern: {
                                type: 'string',
                                description: ( localize(5016, 'The RegExp pattern for beforeText.')),
                                default: '',
                            },
                            flags: {
                                type: 'string',
                                description: ( localize(5017, 'The RegExp flags for beforeText.')),
                                default: '',
                                pattern: '^([gimuy]+)$',
                                patternErrorMessage: ( localize(5018, 'Must match the pattern `/^([gimuy]+)$/`.'))
                            }
                        }
                    },
                    afterText: {
                        type: ['string', 'object'],
                        description: ( localize(
                            5019,
                            'This rule will only execute if the text after the cursor matches this regular expression.'
                        )),
                        properties: {
                            pattern: {
                                type: 'string',
                                description: ( localize(5020, 'The RegExp pattern for afterText.')),
                                default: '',
                            },
                            flags: {
                                type: 'string',
                                description: ( localize(5021, 'The RegExp flags for afterText.')),
                                default: '',
                                pattern: '^([gimuy]+)$',
                                patternErrorMessage: ( localize(5022, 'Must match the pattern `/^([gimuy]+)$/`.'))
                            }
                        }
                    },
                    previousLineText: {
                        type: ['string', 'object'],
                        description: ( localize(
                            5023,
                            'This rule will only execute if the text above the line matches this regular expression.'
                        )),
                        properties: {
                            pattern: {
                                type: 'string',
                                description: ( localize(5024, 'The RegExp pattern for previousLineText.')),
                                default: '',
                            },
                            flags: {
                                type: 'string',
                                description: ( localize(5025, 'The RegExp flags for previousLineText.')),
                                default: '',
                                pattern: '^([gimuy]+)$',
                                patternErrorMessage: ( localize(5026, 'Must match the pattern `/^([gimuy]+)$/`.'))
                            }
                        }
                    },
                    action: {
                        type: ['string', 'object'],
                        description: ( localize(5027, 'The action to execute.')),
                        required: ['indent'],
                        default: { 'indent': 'indent' },
                        properties: {
                            indent: {
                                type: 'string',
                                description: ( localize(5028, "Describe what to do with the indentation")),
                                default: 'indent',
                                enum: ['none', 'indent', 'indentOutdent', 'outdent'],
                                markdownEnumDescriptions: [
                                    ( localize(5029, "Insert new line and copy the previous line's indentation.")),
                                    ( localize(
                                    5030,
                                    "Insert new line and indent once (relative to the previous line's indentation)."
                                )),
                                    ( localize(
                                    5031,
                                    "Insert two new lines:\n - the first one indented which will hold the cursor\n - the second one at the same indentation level"
                                )),
                                    ( localize(
                                    5032,
                                    "Insert new line and outdent once (relative to the previous line's indentation)."
                                ))
                                ]
                            },
                            appendText: {
                                type: 'string',
                                description: ( localize(
                                    5033,
                                    'Describes text to be appended after the new line and after the indentation.'
                                )),
                                default: '',
                            },
                            removeText: {
                                type: 'number',
                                description: ( localize(
                                    5034,
                                    'Describes the number of characters to remove from the new line\'s indentation.'
                                )),
                                default: 0,
                            }
                        }
                    }
                }
            }
        }
    }
};
const schemaRegistry = ( Registry.as(Extensions.JSONContribution));
schemaRegistry.registerSchema(schemaId, schema);
let LanguageConfigurationWorkbenchContribution = class LanguageConfigurationWorkbenchContribution {
    static { this.ID = 'workbench.contrib.languageConfigurationExtensionPoint'; }
    constructor(instantiationService) {
        this.instantiationService = instantiationService;
        this.instantiationService.createInstance(LanguageConfigurationFileHandler);
    }
};
LanguageConfigurationWorkbenchContribution = ( __decorate([
    ( __param(0, IInstantiationService))
], LanguageConfigurationWorkbenchContribution));
registerWorkbenchContribution2(LanguageConfigurationWorkbenchContribution.ID, LanguageConfigurationWorkbenchContribution, WorkbenchPhase.BlockStartup);

export { LanguageConfigurationFileHandler };
