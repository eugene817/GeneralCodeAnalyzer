
import { localize } from '@codingame/monaco-vscode-api/vscode/vs/nls';

const open = ( localize(1842, 'open'));
const close = ( localize(1843, 'close'));
const find = ( localize(1844, 'find'));
var localizedStrings = {
    open: open,
    close: close,
    find: find
};

export { localizedStrings as default };
