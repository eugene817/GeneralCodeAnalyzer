import { IConfigurationService } from "@codingame/monaco-vscode-api/vscode/vs/platform/configuration/common/configuration.service";
export declare namespace PromptsConfig {
    const CONFIG_KEY: string;
    const LOCATIONS_CONFIG_KEY: string;
    const DEFAULT_SOURCE_FOLDER = ".github/prompts";
    const enabled: (configService: IConfigurationService) => boolean;
    const enabledCtx: import("@codingame/monaco-vscode-api/vscode/vs/platform/contextkey/common/contextkey").ContextKeyExpression;
    const getLocationsValue: (configService: IConfigurationService) => Record<string, boolean> | undefined;
    const promptSourceFolders: (configService: IConfigurationService) => string[];
}
